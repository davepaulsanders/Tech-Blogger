<ul class="d-flex justify-content-center align-items-center flex-column p-0">
    {{#each allPosts as |post|}}
    <li class="list-unstyled w-100 col-sm d-flex justify-content-center">
        {{> postInfo post homepage="homepage"}}
    </li>
    {{/each}}
</ul>
